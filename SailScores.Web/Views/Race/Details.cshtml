@using Microsoft.AspNetCore.Routing
@using SailScores.Web.Models.SailScores;
@using SailScores.Core.Model;
@model ClubItemViewModel<Race>

@{
    ViewBag.Title = Model.ClubInitials + " Race";
    Layout = "_ClubLayout";
}

<h2>@Model.ClubInitials.ToUpperInvariant() @Model.Item.Name @Model.Item.Date</h2>

<table class="table">
    <thead>
        <tr>
            <th></th>
            <th>Score</th>
        </tr>
    </thead>
    <tbody>
        @*@foreach (var score in Model.Item)
        {
            <tr>
                <td>
                    @score.Competitor.Name
                </td>
                <td>
                    @score.Value
                </td>

            </tr>
        }*@
    </tbody>
</table>
