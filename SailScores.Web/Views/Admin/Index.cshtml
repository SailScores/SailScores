@model SailScores.Web.Models.SailScores.ClubBaseViewModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_ClubLayout.cshtml";
}

<h2>Index</h2>

<h4>ClubBaseViewModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Index">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ClubInitials" class="control-label"></label>
                <input asp-for="ClubInitials" class="form-control" />
                <span asp-validation-for="ClubInitials" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
